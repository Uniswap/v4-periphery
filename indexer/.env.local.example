# Mainnet RPC (websocket or http). Ponder reads PONDER_RPC_URL_<chainId>.
PONDER_RPC_URL_1=

# Optional: Postgres connection string for production. Defaults to embedded PGlite in dev.
# DATABASE_URL=
